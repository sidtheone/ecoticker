# Storm TDD Report

| # | Location | Gap | Severity | Suggestion |
|---|----------|-----|----------|------------|
| 1 | `tests/run-batch-pipeline.test.ts:416-439` ("skips topic entirely") | AC-1 says "console logs a skip message containing 'skipped' and 'duplicates'". No assertion verifies `console.log` was called with the skip message. The test proves zero inserts, but doesn't verify the observability contract — a silent skip (no log) would also pass. | medium | Add `expect(console.log).toHaveBeenCalledWith(expect.stringContaining("skipped"))` and `expect.stringContaining("duplicates")`. console.log is already spied in `beforeEach`. |
| 2 | `tests/run-batch-pipeline.test.ts:416-439` ("skips topic entirely") | Test does not assert that the scoring LLM call was NOT made. `fetch` is mocked with a third `.mockResolvedValueOnce(makeLLMResponse(SCORING_RESPONSE))` — but nothing checks it was never consumed. The test verifies zero `insertedValues`, but the plan's skip guard says "skip entire topic" which includes skipping the `scoreTopic()` call. A broken implementation that scores but doesn't persist would pass this test. | high | Assert `(global.fetch as jest.Mock).mock.calls` does not include any OpenRouter call after classification. Or assert `fetch` was called exactly 2 times (GNews + classify), not 3. The comment on line 426 says "should NOT be called" but there's no corresponding assertion. |
| 3 | `tests/run-batch-pipeline.test.ts:441-479` ("increments articleCount only by genuinely new articles") | AC-2 says "The article insert loop iterates only `newArticles` (1 iteration, not 2)". The test checks `topicInsert.articleCount === 1` on the INSERT VALUES object (line 478), but this is the initial insert `articleCount` field, not the `onConflictDoUpdate` set clause. The plan's Change 3 modifies the conflict update path: `articleCount: sql\`\${topics.articleCount} + \${newArticles.length}\``. For an existing topic (conflict path), the VALUES `articleCount` is irrelevant — it's the SET clause that matters. The test only covers the no-conflict path. | high | Either: (a) configure `existingTopics` to include "Climate Crisis" so the conflict path fires, then inspect `mock.chain.onConflictDoUpdate` call args to verify the SQL template uses `1` not `2`, or (b) add a separate test for the conflict-update path. Currently, if the implementation fixes VALUES but leaves the SET clause broken, this test passes. |
| 4 | `tests/run-batch-pipeline.test.ts:481-514` ("articlesAdded reflects actual DB inserts") | This test is functionally identical to the partial-duplicate test at line 441 — same setup (2 articles, 1 dupe, same classification), same mock config. The only difference is checking `result.articlesAdded === 1` instead of `topicInsert.articleCount === 1`. These could be collapsed into a single test with both assertions. | low | Merge into the partial-duplicate test or extract shared setup to reduce 30 lines of duplication. Not blocking — but "delete before you add" applies to tests too. |
| 5 | `tests/fix-article-counts.test.ts:36-47` (Contract 1: no mismatches) | The mock returns pre-joined rows with `storedCount` and `actualCount` as fields. This assumes the implementation does a single JOIN query and returns objects with exactly these field names. But the plan says the algorithm is: (1) `SELECT topic_id, COUNT(*) FROM articles GROUP BY topic_id`, (2) `SELECT id, article_count FROM topics`, (3) compare in JS. That's two separate queries, not a join. The test's `mockSelect` configures a single `then` handler — if the implementation makes two sequential SELECTs, the second will get the same mock data. The mock infrastructure (`mockDb.mockSelect`) sets one `then` on the chain, so all awaited queries resolve to the same array. | critical | Either: (a) the implementation must use a single JOIN query (deviation from plan — acceptable if intentional, but Storm flags it), or (b) the test mock must handle two sequential SELECTs with different return values. As written, if the implementation follows the plan's two-query algorithm, the second SELECT (topics) would incorrectly receive the articles GROUP BY result. The stub throws "Not implemented" so we can't verify which approach is intended — but the test is written for a single-query approach while the plan specifies two queries. This mismatch will either force an implementation deviation or cause test failures at green time. Clarify now. |
| 6 | `tests/fix-article-counts.test.ts:127-139` (Contract 6: live mode calls update) | Asserts `mockDb.chain.update` called exactly 2 times for 2 mismatches. But does NOT verify WHAT was updated — no check on the `set` or `where` args. A broken implementation that calls `update` twice with wrong topic IDs or wrong counts would pass. | medium | Add assertion on `mockDb.chain.set` or `mockDb.chain.where` call args to verify the correct `actualCount` is written for the correct `topicId`. |
| 7 | `tests/run-batch-pipeline.test.ts:99-148` (mockForDaily table discrimination) | The mock discriminates `from(articlesTable)` vs `from(topicsTable)` using imported schema references. But batch-pipeline.ts imports schema as `import * as schema from "@/db/schema"` and uses `schema.articles` / `schema.topics`. The test imports `articles as articlesTable, topics as topicsTable` from `@/db/schema` (line 27). These are the SAME references (ES module identity), so the `===` check on line 111 (`tableRef === articlesTable`) will work. However, the mock returns ALL `existingArticleUrls` regardless of what `where()` receives. If the implementation's `inArray` clause has a bug (e.g., wrong column), the mock won't catch it — it ignores the where clause entirely. | low | Acceptable for unit tests (mocks test behavior, not SQL correctness). But worth noting: integration tests against real PostgreSQL are the only way to verify the `inArray(articles.url, fetchedUrls)` clause is correct. Flag for Task 7 verification. |

## Summary

- **AC-1 (all-duplicate skip):** Covered but with gaps — missing skip-log assertion (#1) and missing "scoring was skipped" assertion (#2).
- **AC-2 (partial-duplicate count):** Covered for INSERT path only. The onConflictDoUpdate SET clause (the actual bug site from the plan) is not verified (#3).
- **AC-3 (articlesAdded accuracy):** Covered. Duplicates AC-2 setup (#4, low).
- **AC-4 (correction script):** All 6 contracts present. But the mock strategy assumes a single-query implementation while the plan specifies two queries (#5, critical). Update args not verified (#6, medium).
