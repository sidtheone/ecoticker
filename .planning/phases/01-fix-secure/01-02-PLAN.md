---
phase: 01-fix-secure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package-lock.json
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "npm audit reports zero high or critical vulnerabilities"
    - "All existing tests still pass after dependency updates"
  artifacts:
    - path: "package-lock.json"
      provides: "Updated dependency tree with patched versions"
  key_links: []
---

<objective>
Resolve npm audit vulnerabilities (minimatch high, ajv moderate).

Purpose: Production security hygiene. Both are dev dependencies but flagged by CI audit and Dependabot.

Output: Clean npm audit, updated lockfile, no test regressions.
</objective>

<execution_context>
@/Users/sidhartharora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sidhartharora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run npm audit fix to resolve minimatch and ajv vulnerabilities</name>
  <files>package-lock.json</files>
  <action>
    Run `npm audit fix` to resolve the 2 known vulnerabilities:
    - minimatch (high severity, ReDoS) in @typescript-eslint/typescript-estree, glob
    - ajv (moderate severity, ReDoS) in node_modules/ajv

    Both have fixes available via `npm audit fix` (confirmed by audit output).

    After running audit fix:
    1. Run `npm audit` to verify zero high/critical vulns remain
    2. If any remain, check if they are dev-only deps. Per user decision: "Best effort only -- if transitive deps can't be auto-fixed, accept remaining warnings (dev deps only)"
    3. Do NOT use `npm audit fix --force` (can introduce breaking major version bumps)
    4. Do NOT add overrides to package.json unless npm audit fix handles it naturally (per user decision)
    5. Run the full test suite to verify no regressions from updated packages
  </action>
  <verify>
    <automated>cd /Users/sidhartharora/dev/claude/ecoticker && npm audit fix 2>&1 && npm audit 2>&1 | tail -10 && npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>
    - npm audit reports 0 high/critical vulnerabilities (moderate dev-only warnings acceptable per user decision)
    - package-lock.json updated with patched dependency versions
    - All 604+ tests pass
  </done>
</task>

</tasks>

<verification>
```bash
npm audit 2>&1 | tail -5
npx jest --no-coverage 2>&1 | tail -5
```
</verification>

<success_criteria>
- npm audit shows 0 vulnerabilities (or only acceptable dev-dep warnings per user decision)
- Full test suite passes
- No changes to package.json (only lockfile)
</success_criteria>

<output>
After completion, create `.planning/phases/01-fix-secure/01-02-SUMMARY.md`
</output>
