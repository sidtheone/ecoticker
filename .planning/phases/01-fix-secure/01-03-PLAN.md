---
phase: 01-fix-secure
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified: []
autonomous: false
requirements:
  - PIPE-02
  - PIPE-03

must_haves:
  truths:
    - "Cron endpoint on Railway triggers the batch pipeline and receives a 200 response (not 401)"
    - "Batch run fetches articles from at least one RSS source and inserts them into the database"
    - "Batch run fetches articles from GNews API and inserts them into the database"
    - "Dashboard displays freshly scored topics after a batch run completes"
  artifacts: []
  key_links:
    - from: "Railway cron trigger"
      to: "/api/cron/batch"
      via: "HTTP GET with Bearer CRON_SECRET"
      pattern: "200.*real-data"
---

<objective>
Deploy fixes to Railway and verify the full pipeline works end-to-end in production.

Purpose: PIPE-02 and PIPE-03 cannot be verified locally -- they require the production Railway environment with real API keys, real RSS feeds, and real GNews API. This plan deploys the auth fix + vuln patches and verifies everything works.

Output: Confirmed working production pipeline -- cron triggers batch, RSS and GNews both fetch, dashboard shows fresh data.
</objective>

<execution_context>
@/Users/sidhartharora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sidhartharora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fix-secure/01-01-SUMMARY.md
@.planning/phases/01-fix-secure/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and deploy preparation</name>
  <files></files>
  <action>
    Run the full pre-deploy verification locally:
    1. `npx tsc --noEmit` -- TypeScript must compile cleanly
    2. `npm run build` -- Next.js production build must succeed
    3. `npx jest --no-coverage` -- All tests must pass

    If all pass, push the gsd branch changes. The user will handle merging to main and Railway deployment (Railway deploys from main).

    Note: Do NOT run `git push` to main. Push to the gsd branch only. The user controls the merge-to-main and deploy workflow.
  </action>
  <verify>
    <automated>cd /Users/sidhartharora/dev/claude/ecoticker && npx tsc --noEmit 2>&1 | tail -3 && npm run build 2>&1 | tail -5 && npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>
    - TypeScript compiles without errors
    - Next.js production build succeeds
    - All tests pass
    - Changes pushed to gsd branch
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify production pipeline on Railway</name>
  <files></files>
  <action>
    Human deploys to Railway and verifies the full batch pipeline works end-to-end. Claude cannot access Railway production environment.
  </action>
  <verify>
    <automated>echo "Human verification required -- Railway production deployment"</automated>
  </verify>
  <done>
    - Cron endpoint returns 200 with mode "real-data" on Railway
    - Railway logs show RSS and GNews article fetching
    - Dashboard displays freshly scored topics
  </done>
  <what-built>
    Fixed cron auth bypass (X-API-Key header injection), timing-safe auth comparison, and npm audit vulnerabilities. Code is on the gsd branch, build-verified and test-passing.
  </what-built>
  <how-to-verify>
    1. Merge gsd branch to main and push to trigger Railway deployment
    2. Wait for Railway deployment to complete
    3. In Railway dashboard, manually trigger the cron job (or call the endpoint directly):
       ```
       curl -H "Authorization: Bearer $CRON_SECRET" https://your-app.railway.app/api/cron/batch
       ```
    4. Verify the response is 200 with `"mode": "real-data"` (not 401)
    5. Check Railway logs for:
       - "[1/4] Fetching news..." with RSS feed results
       - GNews API fetch results
       - "[2/4] Classifying articles..."
       - "[3/4] Scoring topics..."
       - "[4/4] Persisting results..."
    6. Visit the dashboard and confirm topics show recent timestamps (today's date)
    7. Confirm at least one topic has articles from RSS sources and at least one from GNews
  </how-to-verify>
  <resume-signal>Type "approved" if pipeline runs successfully on Railway, or describe any issues observed</resume-signal>
</task>

</tasks>

<verification>
Production pipeline verified by human on Railway:
- Cron returns 200 (not 401)
- RSS articles fetched
- GNews articles fetched
- Dashboard shows fresh topics
</verification>

<success_criteria>
- Cron endpoint returns 200 with mode "real-data" on Railway
- Railway logs show both RSS and GNews article fetching
- Dashboard displays freshly scored topics with today's timestamps
</success_criteria>

<output>
After completion, create `.planning/phases/01-fix-secure/01-03-SUMMARY.md`
</output>
